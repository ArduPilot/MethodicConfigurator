MOT_SPIN_ARM,0.1  # Below this value the UAV arms vibrate a lot
MOT_SPIN_MAX,0.9  # According to the manufacturer thrust is mostly linear up-to 90%
MOT_SPIN_MIN,0.13  # MOT_SPIN_ARM + 0.03
MOT_THST_EXPO,0.75  # as per previous drone setup similar specs
